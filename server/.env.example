# 服务端口 (可选，默认 3000)
PORT=3000

# 应用登录密码 (必填)
APP_PASSWORD=your-password-here

# JWT 签名密钥 (必填，同时用于 SESSDATA 加密)
# 生成方式: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-jwt-secret-here
