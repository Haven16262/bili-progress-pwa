node_modules/
dist/
.env
*.db
*.db-*
*.sqlite
server/backups/

# Claude Code 自动生成（临时产物，不纳入版本控制）
.claude/plans/

# 外部引入的第三方 skills（emilkowalski/skills 等），非项目产物
.claude/skills/

# 迁移后待办（含部署细节，不公开）
KNOWN-ISSUES.md

# /design 生成的 canvas 编辑器 payload（2MB 压缩产物，可由 canvas-src/*.dc.html 经 seed 脚本重生）
plans/ui-refresh/canvas-src/bili-pwa-visual-refresh.html
